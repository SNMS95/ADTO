[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "adto"
version = "0.1.0"
description = "Automatic Differentiation for Topology Optimization"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]